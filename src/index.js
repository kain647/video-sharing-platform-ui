import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Video from "./video-platform-ui";

ReactDOM.render(
  <React.StrictMode>
      <Video/>
  </React.StrictMode>,
  document.getElementById('root')
);
